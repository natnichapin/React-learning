import React from 'react';
import ReactDOM from 'react-dom/client'; // แค่ดึงมาจาก dependency 
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App /> /* For the moment we want to get edit ( 1.HTML in javascript browser ไม่เข้าใจ)*/

); //รัน ผลลัพธ์ของ App


